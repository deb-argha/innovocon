PORT=5000
MONGO_URI=mongodb://localhost:27017/slum-mapping
# For quick testing, you can use MongoDB Atlas free tier
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/slum-mapping
# Or use MongoDB Atlas quick setup, which gives you a ready-to-use connection string

# For a hackathon, you can use a simple JWT secret
JWT_SECRET=your_jwt_secret_key

# OpenStreetMap related configs (if needed)
OSM_API_URL=https://api.openstreetmap.org